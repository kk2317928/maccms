# MACCMS Headless AI 使用說明

> 適用分支：`feature/headless-ai-v1`  
> 文件版本：2026-09-23 / T-118 測試包  
> 建議環境：PHP 8.1、MySQL 5.7 或 8.0、Nginx / Apache  
> Public ID：6 位大小寫敏感英數字，格式 `[A-Za-z0-9]{6}`

## 1. 安裝後先做什麼

完成 Web Installer 後，先登入後台並確認「智能內容工作區」存在。若是既有網站升級，先備份資料庫及 `application/`、`upload/`，再執行：

```bash
php think maccms:migrate
php think maccms:migrate
```

目前版本共有 23 個版本化 migration；第二次執行應顯示 `applied=0, skipped=23`。然後確認 Cron 可以執行 `php think`。

T-118 測試包已驗證 `upload/.gitkeep`、`vendor/autoload.php`、Captcha runtime、migrations、CLI、MySQL 5.7/8.0、原生影片路徑及完整內容 lifecycle 都能從解壓後的 ZIP 執行。

## 2. Key／Secret 到底填在哪裡

以下先列出**實際設定位置**。不同 Key 並不是全部填在同一個後台頁面。

| 用途 | 建議設定位置 | 實際讀取位置 |
|---|---|---|
| 內容 AI Key | 後台 → 系統 → AI 內容標註配置（`system/configaicontent`）；智能內容工作區亦有入口 | `config('maccms')['ai_content']` |
| TMDB Key | 後台 → 系統 → AI 搜尋配置（`system/configaisearch`）→ TMDB；智能內容工作區亦有入口 | `ai_search.external_sources.sources.tmdb.api_key` |
| API v1 JWT Secret | **後台 → 系統 → 網站參數配置 → API JWT**，或環境變數 | `app.api_v1_jwt_secret` / `MACCMS_API_V1_JWT_SECRET` |
| Import API Secret | PHP-FPM / CLI 環境變數 | `MACCMS_CONTENT_IMPORT_SECRET` |
| Playback HMAC Secret | PHP-FPM / CLI 環境變數 | `MACCMS_API_V1_PLAYBACK_SECRET` |
| Playback hostname allowlist | `application/extra/maccms.php` 的 `app.api_v1_playback_sources` | Playback policy |
| CORS Origins | `application/extra/maccms.php` 的 `app.api_v1_cors_origins` | API delivery policy |
| S3 Key | MACCMS 原有上傳／遠端儲存設定 | 原生 upload provider / poster ingestion |

### 2.1 內容 AI Key：後台可直接輸入

登入後台，打開「智能內容工作區」→「輸入或更新 AI API Key」，或從系統設定進入「AI 內容標註配置」（`system/configaicontent`）。啟用內容 AI，選擇 Provider、Model，然後在密碼欄位輸入 API Key 並儲存。欄位不會回填完整 Key；留空儲存時會保留原有值。

若要沿用「AI 搜尋配置」中的 Key，可勾選 `use_ai_search_credentials`。只有內容 AI 自身的 Key 為空且啟用繼承時，才會使用 `ai_search.api_key`。智能內容工作區只顯示是否已設定，不會顯示 Key 內容。

### 2.2 TMDB Key：後台可直接輸入

打開「智能內容工作區」→「輸入或更新 TMDB API Key」，或到「系統 → AI 搜尋配置 → TMDB」（`system/configaisearch#tmdb-settings`）。啟用 TMDB，在密碼欄位輸入 TMDB API Key 並儲存；留空儲存時會保留原有值。需要對應的系統設定權限才能修改。

AI 和 TMDB 的設定最終儲存在伺服器的 `application/extra/maccms.php`；不要將真實 Key 寫入 Git 或公開前端。

### 2.3 API JWT 可以在後台填

這一項目前已有後台 UI：

```text
後台
→ 系統
→ 網站參數配置
→ API JWT
```

可設定：

- API JWT 開關；
- JWT Secret；
- TTL；
- Issuer。

Secret 欄位留空重新儲存時，程式會保留原 Secret；啟用時若 Secret 太弱，後端亦有安全檢查。

Headless API v1 另外也支援：

```bash
MACCMS_API_V1_JWT_SECRET="至少 32 bytes"
```

注意：這裡是**新版 API v1 Session/JWT**。不要和頁面上其他舊版 MACCMS API/JWT 功能混為一談。

### 2.4 Import / Playback Secret 不在後台填

這兩個屬於 server-to-server secret，應放在 Web Server / PHP-FPM 與 CLI 執行環境：

```bash
MACCMS_CONTENT_IMPORT_SECRET="至少 32 bytes"
MACCMS_API_V1_PLAYBACK_SECRET="至少 32 bytes"
```

例如 systemd PHP-FPM 可透過受保護的 EnvironmentFile 注入；Docker / Compose 則使用 secrets 或環境變數。修改後要重新載入 PHP-FPM，並確認 Cron 執行 `php think` 時也能取得相同環境變數。

**不要**把這兩個 Secret 填進 Next.js 的 `NEXT_PUBLIC_*`。

### 2.5 CORS / Playback allowlist 在哪裡

目前同樣直接設定：

```text
application/extra/maccms.php
```

例如：

```php
'app' => [
    // 保留原本 app 內所有設定
    'api_v1_cors_origins' => [
        'https://www.example.com',
    ],
    'api_v1_playback_sources' => [
        'dplayer' => [
            'enabled' => true,
            'allowed_hosts' => [
                'media.example.com',
                'cdn.example.com',
            ],
        ],
    ],
    'api_v1_playback_signing' => [
        'enabled' => true,
        'ttl' => 300,
    ],
],
```

不要另外建立第二個 `'app' => [...]` 區塊；應加入現有 `app` 陣列內。

### 2.6 設定完成後如何確認

先確認 CLI：

```bash
php think list
php think maccms:jobs --max-jobs=1 --max-seconds=10 --lease-seconds=120 --worker=config-test
```

再檢查：

```text
GET /api/v1/openapi.json
GET /api/v1/site-config
```

AI / TMDB 則建議用一部測試影片，在影片管理／智能內容工作區執行單片 rerun，確認 job 成功後再開啟批量 Cron。

---

## 4. AI Key 詳細設定

新版內容 AI 在後台「AI 內容標註配置」編輯，儲存於 `ai_content`。支援：

- `openai`
- `claude`
- `gemini`
- `deepseek`
- `qwen`
- `glm`

主要設定欄位：

```php
'ai_content' => [
    'enabled' => '1',
    'provider' => 'openai',
    'model' => 'gpt-4o-mini',
    'api_base' => 'https://api.openai.com/v1',
    'api_key' => '你的 API Key',
    'timeout' => 30,
    'max_tokens' => 800,
    'batch_size' => 20,
    'daily_budget_micros' => 0,
    'auto_adopt_empty' => '0',
    'prompt_version' => 'normalize-v1',
    'retry_count' => 2,
    'retry_delay_ms' => 500,
],
```

`api_base` 留空時會按 provider 使用預設官方 API Base。若已有 `ai_search` Key，也可以使用 `use_ai_search_credentials=1` 繼承其 `api_key` / `api_base`，避免重複填寫。

`daily_budget_micros=0` 表示不設每日成本上限。正式站建議先設定小額限制並觀察工作區的 Token / 成本統計。API Key 不應提交到 Git，也不要寫入公開前端。

AI 不會直接無條件覆蓋人工欄位。人工修改具有較高優先權及 lock；AI 結果會進入審核流程。

## 4. TMDB Key 設定

TMDB 在後台「AI 搜尋配置 → TMDB」編輯，Provider 需要：

```php
[
    'enabled' => '1',
    'api_key' => '你的 TMDB API Key',
    'base_url' => 'https://api.themoviedb.org/3',
    'image_base_url' => 'https://image.tmdb.org/t/p/w500',
    'language' => 'zh-TW',
    'region' => 'TW',
]
```

`enabled=1` 且 `api_key` 非空才會真正啟用。TMDB 工作包括搜尋候選、手動指定 ID、抓取詳細資料，以及經審核後採用欄位。

TMDB 不會自動合併重複影片，也不應直接覆蓋人工鎖定欄位。海報採用後會走 hardened ingestion，再依 S3 設定保存；S3 失敗時不會把失敗的本地路徑偽裝成成功 URL。

## 5. S3 與海報

先在 MACCMS 原有上傳 / S3 設定中配置 Bucket、Endpoint、Access Key、Secret Key、公開 URL 等資料，再測試一般圖片上傳。

TMDB 海報流程成功後會維護：

- `vod_pic`
- `old_poster_s3`
- `poster_s3`

建議先用測試 Bucket 驗證。不要直接用正式 Bucket 做首次測試。

## 6. 背景工作與 Cron

AI、TMDB、海報 ingestion 等耗時工作不在後台 HTTP request 中直接長時間執行，而是進入 `content_job`。

建議每分鐘：

```cron
* * * * * cd /srv/maccms/current && /usr/bin/php think maccms:jobs --max-jobs=20 --max-seconds=50 --lease-seconds=120 --worker=cron-main >> /var/log/maccms/jobs.log 2>&1
```

統計可另外執行：

```cron
*/5 * * * * cd /srv/maccms/current && /usr/bin/php think maccms:analytics >> /var/log/maccms/analytics.log 2>&1
```

手動測試：

```bash
php think maccms:jobs --max-jobs=20 --max-seconds=50 --lease-seconds=120 --worker=manual
php think maccms:analytics
```

如果影片一直停在 AI / TMDB 階段，先看工作區的 queue、failure summary、worker heartbeat，而不是反覆按執行按鈕。

## 7. 自動內容流程

工作區入口為 `content_workspace/view`。影片「多語言／進階資料」中可查看 `title_tw`、`title_cn`、`title_en` 與唯讀的六位 Public ID。

原生後台新增、採集或受保護 Import API 建立影片後，主要流程為：

```text
ai_processing
→ duplicate_review
→ tmdb_matching
→ manual_review
→ published
```

影片編輯頁可查看 AI、TMDB、Duplicate 狀態，也可對單片重新執行 AI / TMDB。

分類採用詞典制度。AI 提出的 Region / Genre / Tag 只有在匹配既有啟用詞條並經審核後才會成為正式關聯；未知詞不會直接污染分類資料。

## 8. 重複影片

系統會建立 duplicate candidates，但**不會自動合併**。

管理員可：

- 比較兩筆影片；
- 標記為不同作品；
- 選擇主要影片並合併；
- 查看 snapshot；
- 在沒有衝突時還原。

原生 `vod_repeat` 重名快取可檢查 / 修復：

```bash
php think maccms:repair-vod-repeat
php think maccms:repair-vod-repeat --confirm
```

## 9. Public ID

每部影片除了內部 `vod_id`，另有公開 6 位 ID：

```text
Ab12Cd
aB12cD
000001
Z9x8Q2
```

大小寫敏感，因此 `Ab12Cd` 與 `ab12cd` 是不同 ID。API、影片 Detail、播放及 canonical redirect 都應保留原始大小寫。

舊資料如需修復 Public ID，使用目前版本提供的 Public ID repair CLI；先 dry-run，再依 CLI 提示確認正式修改。不要自行用 SQL 大量改 `vod_ext.public_id`。

## 10. API v1 總覽

新安裝和瀏覽器驗收依照 `docs/testing/native-smoke-checklist.md` 執行。先前的 `headless-ai-v1.0.3-rc1` 只能用於測試；本分支的正式版本仍需通過目前提交的完整回歸、套件與手動驗收。



OpenAPI 是 API 的最終欄位與 request/response 參考：

```text
GET /api/v1/openapi.json
```

主要公開 API：

```text
GET  /api/v1
GET  /api/v1/home
GET  /api/v1/videos
GET  /api/v1/videos/{public_id}
GET  /api/v1/videos/{public_id}/episodes
GET  /api/v1/videos/{public_id}/recommendations
GET  /api/v1/search
GET  /api/v1/taxonomies
GET  /api/v1/rankings
GET  /api/v1/people/{slug}
GET  /api/v1/site-config
POST /api/v1/events
```

會員 API：

```text
POST   /api/v1/auth/login
POST   /api/v1/auth/refresh
POST   /api/v1/auth/logout
GET    /api/v1/auth/sessions
DELETE /api/v1/auth/sessions/{session_id}

GET    /api/v1/me/favorites
PUT    /api/v1/me/favorites/{public_id}
DELETE /api/v1/me/favorites/{public_id}
GET    /api/v1/me/history
GET    /api/v1/me/progress/{public_id}
PUT    /api/v1/me/progress/{public_id}
DELETE /api/v1/me/progress/{public_id}
POST   /api/v1/me/activity/merge
```

播放：

```text
GET /api/v1/videos/{public_id}/playback/{source_id}/{episode_id}
```

例如：

```text
/api/v1/videos/Ab12Cd/playback/s1/s1e1
```

## 11. API v1 JWT Secret

API v1 使用獨立 secret，不要與舊 MACCMS JWT 共用。至少 32 bytes：

```bash
openssl rand -base64 48
```

推薦透過 PHP-FPM / CLI 環境變數：

```bash
MACCMS_API_V1_JWT_SECRET="請換成真正的隨機 Secret"
```

程式也支援 `app.api_v1_jwt_secret`；環境變數方式較適合部署 secrets。

Access Token 為短期 token；Refresh Token 使用可撤銷、可輪換的 device session。前端遇到 Access Token 過期時應呼叫 `POST /api/v1/auth/refresh`，不要要求使用者每次重新登入。

## 12. CORS

Next.js 等獨立前端若與 API 不同 Origin，需要加入精確 HTTPS Origin，例如：

```php
'app' => [
    'api_v1_cors_origins' => [
        'https://www.example.com',
        'https://app.example.com',
    ],
],
```

不要使用 `*` 代替正式 Origin。

## 13. Playback API

播放 API 採來源白名單。概念設定：

```php
'app' => [
    'api_v1_playback_sources' => [
        'dplayer' => [
            'enabled' => true,
            'allowed_hosts' => [
                'media.example.com',
                'cdn.example.com',
            ],
        ],
    ],
],
```

只有啟用的 source 且 URL hostname 在 allowlist 才會返回播放資料。

若啟用 HMAC signing：

```bash
MACCMS_API_V1_PLAYBACK_SECRET="至少 32 bytes 的 Secret"
```

並設定：

```php
'api_v1_playback_signing' => [
    'enabled' => true,
    'ttl' => 300,
],
```

Playback Secret 只能放在 MACCMS Server 與可信任的 Next.js Server，不可放進 browser bundle 或 `NEXT_PUBLIC_*`。

## 14. 新增影片 Import API

新增的受保護入口：

```text
POST /api/v1/import/videos
```

它不使用會員 Bearer Token，而是獨立 HMAC secret：

```bash
MACCMS_CONTENT_IMPORT_SECRET="至少 32 bytes 的 Secret"
```

每次請求需要：

- `X-Import-Timestamp`：10 位 Unix timestamp；
- `X-Import-Nonce`：12–128 位英數 / `._:-`；
- `X-Import-Signature`：HMAC-SHA256 hex；
- `Idempotency-Key`；
- JSON body。

簽名 canonical string：

```text
{timestamp}
{nonce}
POST
/api/v1/import/videos
{sha256(raw_request_body)}
```

然後：

```text
signature = HMAC-SHA256(canonical_string, MACCMS_CONTENT_IMPORT_SECRET)
```

Timestamp 容許誤差預設 300 秒；nonce 只能使用一次。同一 `Idempotency-Key` 搭配完全相同 payload 可安全重試；同一 key 換 payload 會被拒絕。

實際可匯入欄位、播放來源 payload 與 response 請以 `/api/v1/openapi.json` 為準，不要直接提交 `vod_id`、發布狀態或合併控制欄位。

## 15. API 呼叫範例

公開影片：

```bash
curl "https://api.example.com/api/v1/videos/Ab12Cd?locale=zh-TW"
```

登入：

```bash
curl -X POST "https://api.example.com/api/v1/auth/login" \
  -H "Content-Type: application/x-www-form-urlencoded" \
  --data-urlencode "user_name=test" \
  --data-urlencode "user_pwd=your-password" \
  --data-urlencode "device_name=web"
```

取得 Access Token 後：

```bash
curl "https://api.example.com/api/v1/me/favorites" \
  -H "Authorization: Bearer YOUR_ACCESS_TOKEN"
```

完整 JSON schema、HTTP status、pagination 及錯誤碼以 OpenAPI 為準。

## 16. API v1 給 Next.js 使用

建議：

```text
Browser
  ↓
Next.js
  ↓
MACCMS /api/v1
```

公開 catalog API 可由 Browser 或 Next.js 呼叫；涉及 Playback Secret、Import Secret 或其他伺服器 Secret 的操作只能在 Server Route / Server Action 執行。

Next.js 不應知道：

- MySQL 密碼；
- AI API Key；
- TMDB API Key；
- S3 Secret Key；
- `MACCMS_CONTENT_IMPORT_SECRET`；
- `MACCMS_API_V1_PLAYBACK_SECRET`。

## 17. 修復既有資料

內容工作流：

```bash
php think maccms:repair-content --limit=100
php think maccms:repair-content --apply --confirm=REPAIR --limit=100
```

先 dry-run。已發布、已合併、人工審核中或人工鎖定影片會 fail closed / skip，不應強行批次覆蓋。

重名快取：

```bash
php think maccms:repair-vod-repeat
php think maccms:repair-vod-repeat --confirm
```

Migration：

```bash
php think maccms:migrate
```

## 18. 常見問題

### AI 已填 Key，但沒有執行

確認：

1. `ai_content.enabled=1`；
2. provider / model 正確；
3. Key 有效；
4. Cron 正在執行 `maccms:jobs`；
5. daily budget 未耗盡；
6. 工作區沒有顯示 terminal failure。

### TMDB 找不到資料

確認 `enabled=1`、API Key、語言 / Region，然後可在影片或 TMDB 工作區手動指定 TMDB ID / rerun。

### API 登入一直 500 / 503

確認 `MACCMS_API_V1_JWT_SECRET` 至少 32 bytes，`runtime/` 可寫，資料庫 migration 已完成。

### Import API 一直拒絕

確認 Server / Client 時鐘同步、timestamp 在 300 秒內、nonce 沒重用，而且簽名用的是**實際傳送 body 的原始 bytes**。

### mixed-case Public ID 404

不要把 Public ID `strtoupper()` / `strtolower()`。例如 `Ab12Cd` 必須原樣傳送。

### 驗證碼 Class not found

確認：

```text
vendor/topthink/think-captcha/src/Captcha.php
```

存在，而且部署工具沒有排除 `vendor/`。

## 19. 正式上線前檢查

至少完成：

- Web Installer 人工驗收；
- PHP regression；
- MySQL 5.7 / 8.0 release matrix；
- rollback rehearsal；
- T-118 實際 ZIP 解壓安裝驗收；
- AI / TMDB 測試 Key；
- S3 測試 Bucket；
- Cron heartbeat；
- Duplicate merge + restore；
- mixed-case Public ID；
- API v1 OpenAPI / login / playback；
- Import API HMAC；
- 備份與還原演練。

所有 secrets 應由部署環境或受保護設定保存，不應提交至 Git。
