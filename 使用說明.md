# MACCMS Headless AI 使用說明

> 適用分支：`feature/headless-ai-v1`  
> 測試版本：`v1.0.3-rc1`  
> 執行環境：PHP 8.1、MySQL 5.7／8.0、Nginx 或 Apache  
> 狀態：測試版。自動化回歸已通過，但全新 Web 安裝及瀏覽器操作仍須在獨立測試環境人工驗收。

## 1. 功能概要

本版本保留 MACCMS 原生影片、採集、會員及播放機制，另外提供：

- 繁體中文、簡體中文、英文及原始片名；
- 曾用片名、第二分類 `type2`；
- TMDB 配對、預告片、預覽及 S3 海報欄位；
- AI 片名整理與欄位審核；
- 重複影片比較、人工合併及還原；
- 智能內容工作區及背景任務；
- 獨立的 `/api/v1`；
- 六位公開影片 ID、排行榜、推薦、收藏、觀看紀錄及進度；
- 移除非必要的 MACCMS 官方更新、公告、聯盟及遙測連線。

原生影片資料仍以 `vod`、`vod_play_*` 為相容來源；新增欄位保存在 `vod_ext`，不會直接改變原生 `vod` 欄位結構。

## 2. 安裝前準備

### 2.1 環境需求

- PHP 8.1；
- MySQL 5.7 或 8.0，資料庫字元集建議使用 `utf8mb4`；
- PHP 擴充：PDO、pdo_mysql、mbstring、curl、fileinfo、zip、xml；
- Web 伺服器可將網站根目錄指向程式目錄；
- CLI PHP 與 Web PHP 使用相同版本及設定；
- Cron 可在程式目錄執行 `php think`。

### 2.2 必須可寫入的項目

安裝前確認 Web 執行帳號可寫入：

- `application/database.php`
- `application/route.php`
- `application/extra/`
- `application/data/backup/`
- `application/data/update/`
- `runtime/`
- `upload/`

不要把整個網站設為 `777`。應由部署帳號設定正確擁有者及最小必要權限。

### 2.3 安全準備

- 使用全新、空白的測試資料庫；
- 不要使用正式站資料、API Key、會員資料或媒體檔案測試；
- 將預設的 `admin.php` 改成不易猜測的檔名，例如 `manage_x7.php`；
- 正式環境必須使用 HTTPS；
- 安裝及測試期間先停用 SMTP、SMS、支付、推送、採集、AI、TMDB 等外部連線。

## 3. 全新安裝

1. 解壓測試包，將內層 `maccms-headless-ai-v1.0.3-rc1/` 內容放到網站目錄。
2. 確認 `upload/.gitkeep`、`vendor/topthink/think-captcha/src/Captcha.php` 及 `application/data/migrations/` 存在。
3. 建立空白資料庫及專用資料庫帳號。
4. 瀏覽 `https://你的網域/install.php`。
5. 完成環境檢查、資料庫設定及管理員建立。
6. 安裝器會先匯入 MACCMS 原生資料表，再自動執行所有版本化遷移，最後才建立 `install.lock`。
7. 安裝完成後進入已重新命名的管理入口。
8. 確認後台可看到「智能內容工作區」。
9. 確認資料庫存在 `vod_ext`、`schema_migration`、`content_job` 等擴充資料表。

若擴充遷移失敗，安裝程序不得視為成功。請保留錯誤紀錄、清空測試資料庫後再重試，不要手動建立不完整的資料表。

## 4. 既有網站升級

升級前先停止 Cron，進入維護狀態，並備份：

- 完整資料庫；
- `application/database.php`；
- `application/extra/`；
- `application/data/`；
- `upload/`；
- 已重新命名的管理入口檔；
- Nginx／Apache、PHP-FPM 及排程設定。

部署新程式時不要覆蓋正式環境的資料庫密碼或 API 密鑰。完成檔案切換後執行：

```bash
php think maccms:migrate
php think maccms:migrate
```

第一次會套用尚未執行的遷移；第二次應顯示沒有新的遷移。遷移具有 checksum 保護，已套用的 SQL 檔不可修改，修正資料庫結構時必須新增下一個版本的遷移。

大型 `vod` 表轉換為 InnoDB 可能需要較長時間及額外磁碟空間，應先在副本估算並安排維護時段。

## 5. 影片多語言與進階欄位

進入「影片管理」→新增或編輯影片，開啟「多語言／進階資料」分頁。

| 欄位 | 用途 |
|---|---|
| 繁體中文 | `title_tw` |
| 簡體中文 | `title_cn` |
| 英文片名 | `title_en` |
| 原始片名 | `original_title` |
| 曾用片名 | 每行一個，儲存為 JSON |
| 第二分類 | `type2`，例如 `kr-drama`、`jp-drama`、`tw-drama` |
| TMDB 類型 | `movie` 或 `tv` |
| TMDB ID | TMDB 數字 ID |
| 預告片 URL | 正式預告片地址 |
| 預覽 URL | 短預覽地址 |
| 舊海報 | 舊海報來源 |
| 舊海報 S3 | 舊海報 S3 地址 |
| 海報 S3 | 目前使用的 S3 海報地址 |

人工修改的擴充欄位會透過 `FieldGovernance` 標記為 `manual` 並鎖定。AI、TMDB 或再次匯入不得在未經人工確認時覆蓋這些欄位。

若舊版表單或第三方工具沒有提交 `vod_ext`，系統會保留原有擴充資料，不會把欄位清空。

## 6. 智能內容工作區

超級管理員可直接使用。其他管理員必須配置相應權限：

| 功能 | 權限 |
|---|---|
| 工作區首頁 | `content_workspace/view` |
| AI 欄位審核 | `content_workspace/review` |
| 重複影片合併／還原 | `content_workspace/merge_restore` |
| 執行 AI 工作 | `content_workspace/run_ai` |
| 執行 TMDB 工作 | `content_workspace/run_tmdb` |
| 最終發布 | `content_workspace/publish` |

主要流程：

1. 影片匯入或採集；
2. AI 正規化；
3. 人工處理重複候選；
4. TMDB 搜尋、人工選擇或標示無匹配；
5. 審核欄位及鎖定人工結果；
6. 預覽驗證；
7. 人工確認發布。

重複影片不會自動合併。合併及還原均須明確確認、正確權限及審計紀錄。還原時若偵測到合併後資料已變更，系統會拒絕覆蓋。

## 7. AI 與 TMDB 設定

先在後台完成 AI／內容服務設定，再啟用相關工作。測試環境應使用測試 Key 或本地 Stub。

注意：

- API Key 不得提交到 Git；
- 不得把完整供應商回應、Token、Authorization Header 寫入日誌；
- 大量工作只會進入 `content_job` 佇列，不會在管理頁請求內長時間執行；
- 人工鎖定欄位優先於 AI、TMDB 及匯入資料；
- TMDB 候選需要人工確認後才可採用。

## 8. Cron 與背景工作

建議排程：

```cron
* * * * * cd /srv/maccms/current && /usr/bin/php think maccms:jobs --max-jobs=20 --max-seconds=50 --lease-seconds=120 --worker=cron-main >> /var/log/maccms/jobs.log 2>&1
*/5 * * * * cd /srv/maccms/current && /usr/bin/php think maccms:analytics >> /var/log/maccms/analytics.log 2>&1
```

手動執行：

```bash
php think maccms:jobs --max-jobs=20 --max-seconds=50 --lease-seconds=120 --worker=manual-test
php think maccms:analytics
```

避免多個排程重疊執行。應監控非零結束碼、過期 lease、佇列積壓、重複失敗、心跳過期及日誌無法寫入。

## 9. API v1

OpenAPI 文件：

```text
GET /api/v1/openapi.json
```

常用驗證端點：

- `POST /api/v1/auth/login`
- `POST /api/v1/auth/refresh`
- `POST /api/v1/auth/logout`
- `GET /api/v1/auth/sessions`
- `DELETE /api/v1/auth/sessions/{session_id}`

設定獨立 JWT Secret，至少 32 個隨機位元組：

```bash
openssl rand -base64 48
```

將結果放入 PHP-FPM 與 CLI 環境變數 `MACCMS_API_V1_JWT_SECRET`。不要與舊版 JWT 共用，也不要提交到儲存庫。

跨來源前端必須設定精確的 HTTPS Origin；不支援萬用字元：

```php
$GLOBALS['config']['app']['api_v1_cors_origins'] = array(
    'https://app.example.com',
);
```

播放 API 預設停用。啟用時必須逐一設定來源及 CDN hostname allowlist。如啟用 HMAC 簽署，將至少 32 位元組的 `MACCMS_API_V1_PLAYBACK_SECRET` 同時配置於 PHP 及可信任的 Next.js 伺服器；不可放進瀏覽器 JavaScript 或公開環境變數。

## 10. 測試版驗收

請只在獨立環境測試，至少完成：

1. 空白資料庫 Web 安裝；
2. 管理員登入及權限限制；
3. 建立影片並儲存繁中、簡中、英文、TMDB、預告及海報欄位；
4. 重新開啟影片，確認欄位完整；
5. 使用舊式、沒有 `vod_ext` 的請求更新原生欄位，確認擴充資料沒有被清空；
6. 開啟智能內容工作區；
7. 建立佇列工作並由 `maccms:jobs` 處理；
8. 測試重複影片合併及還原；
9. 測試 `/api/v1/openapi.json`、公開列表、登入及播放限制；
10. 檢查日誌沒有秘密、PHP Warning、SQL 錯誤及非預期外連。

完整人工清單位於 `docs/testing/native-smoke-checklist.md`。每項須記錄 `PASS`、`FAIL`、`BLOCKED` 或 `NOT_RUN`，不可用自動化測試取代人工結果。

## 11. 備份與回復

正式升級必須保存應用程式 SHA、安裝包 SHA-256、資料庫備份、上傳檔案快照及上一版本安裝包。

發生問題時：

1. 停止 Cron；
2. 啟用維護模式；
3. 保存錯誤日誌；
4. 切回上一個不可變版本；
5. 評估新遷移是否與舊程式相容；
6. 必要時把備份還原到新命名的資料庫，不要覆蓋唯一的原始副本；
7. 完成首頁、後台、播放、API、資料庫及佇列健康檢查後再恢復流量。

本專案沒有自動 down migration。不要臨時撰寫反向 SQL 直接操作正式資料庫。

## 12. 常見問題

### 安裝時提示 `./upload` 不存在或不可寫

確認測試包包含 `upload/.gitkeep`，並讓 Web 執行帳號擁有 `upload/` 的寫入權限。

### 驗證碼出現 `Class 'think\\captcha\\Captcha' not found`

確認 `vendor/topthink/think-captcha/src/Captcha.php` 存在，且部署時沒有排除 Composer／vendor 依賴。

### 後台沒有「智能內容工作區」

確認使用目前測試包、清理應用快取，並檢查管理員是否具有 `content_workspace/view` 權限。超級管理員 ID 1 可直接進入。

### 看不到「多語言／進階資料」

確認目前使用 `application/admin/view_new/`，並重新開啟影片新增／編輯頁。不要部署已淘汰的 `application/admin/view/`。

### `vod_ext` 不存在

全新安裝代表遷移未完成；不要繼續使用該資料庫。既有網站請先備份，再執行：

```bash
php think maccms:migrate
```

### AI／TMDB 工作一直沒有執行

確認 Cron 正在執行 `php think maccms:jobs`、CLI 使用正確資料庫設定、供應商設定已啟用，並檢查工作區的佇列深度、失敗摘要及 worker heartbeat。

### API v1 回傳 503

確認 `runtime/` 可寫。API rate limiter 若無法安全開啟或鎖定狀態檔，會 fail closed 回傳 503。

## 13. 正式發布條件

在建立正式 `v1.0.3` 標籤前，必須：

- 完成 MySQL 5.7 與 8.0 自動化矩陣；
- 完成 PHP regression 及 rollback rehearsal；
- 在空白資料庫完成人工 Web 安裝；
- 驗證多語言欄位儲存及重新讀取；
- 驗證智能內容工作區入口及權限；
- 驗證舊式表單不會清空擴充資料；
- 記錄測試環境、提交 SHA、結果及證據；
- 確認沒有 Critical／Important 審查問題。

未完成上述人工驗收前，`v1.0.3-rc1` 只能用於測試，不應部署到正式網站。


## 14. 完整內容工作流與既有資料修復

### 14.1 自動工作流

影片經原生後台新增、採集新增或受保護匯入 API 建立後，會依序進入：

`ai_processing → duplicate_review → tmdb_matching → manual_review → published`

AI 只提出欄位及分類候選。分類候選必須匹配既有啟用詞條並由管理員接受，才會寫入正式 `vod_meta_term` 關聯；系統不會自動建立未知詞條。重複候選必須由管理員標示為不同作品，或明確執行合併。TMDB 候選亦須選擇候選或標示無匹配，才能進入最終人工審核。

### 14.2 受保護匯入 API

`POST /api/v1/import/videos` 使用獨立 HMAC 憑證，不使用會員 Access Token。部署時設定至少 32 位元組的 `MACCMS_CONTENT_IMPORT_SECRET`，每次請求需提供時間戳、一次性 nonce、`Idempotency-Key` 及依 OpenAPI 規格計算的簽章。相同冪等鍵與相同 payload 會返回原結果；相同鍵搭配不同 payload 會被拒絕。匯入 API 不接受發布或合併欄位。

公開補充端點：

- `GET /api/v1/people/{slug}`：只返回人物及其已發布、未合併影片，不暴露內部 ID。
- `GET /api/v1/site-config`：只返回公開白名單設定，不含資料庫、郵件、AI、TMDB、S3、JWT 或播放簽章秘密。

### 14.3 修復既有影片

先執行 dry-run：

```bash
php think maccms:repair-content --limit=100
```

確認計數後才正式套用：

```bash
php think maccms:repair-content --apply --confirm=REPAIR --limit=100
```

每批限制為 1–500。命令會建立缺少的擴充／工作流狀態、建立待審核分類建議及補入缺少的 AI 工作。已發布、已合併、人工審核中或具有人工鎖定的影片會跳過。命令可重複執行；完成列不會阻塞後續批次，亦不會重複建立工作或分類關聯。

### 14.4 合併與還原

版本 2 合併快照涵蓋播放來源、曾用片名、多語資料、正式分類、外部來源映射及欄位 provenance。還原前會驗證快照 hash 及目前合併投影；如合併後資料已被修改，系統會拒絕覆寫，需先人工處理衝突。

### 14.5 故障排查

- 工作停在 `ai_processing`：檢查 AI job 狀態、每日預算、worker heartbeat 及安全錯誤摘要。
- 工作停在 `duplicate_review`：確認所有 pending 候選已標示不同作品或完成合併。
- 工作停在 `tmdb_matching`：確認 TMDB 工作完成，並在工作區選擇候選或標示無匹配。
- 無法發布：處理所有待決 AI／分類項目，確認有效地區與分類、播放來源 allowlist、六位公開 ID 及 `manual_review` 狀態。
- 還原被拒絕：保留目前資料與快照，不要直接修改快照 JSON；先比較合併後變更及審計紀錄。
- 匯入回傳重播／簽章錯誤：確認 Web 與客戶端時間同步、nonce 未重用、body digest 與實際傳送位元完全一致。
